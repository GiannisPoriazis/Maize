export interface AppSettings {
  userId: number;
  language: string;
  fontsize: number;
  timezone: number;
  theme: number;
  grid_theme: number;
  notification_sound: number;
  notification_alerts: number;
  pin: string;
  lock_screen: number;
  lock_screen_timeout: number
}
